{% if room.images and room.images|length > 0 %}
    <img src="{{ url_for('static', filename='uploads/' ~ room.images[0][0]) }}">
{% else %}
    <img src="{{ url_for('static', filename='uploads/default.jpg') }}">
{% endif %}